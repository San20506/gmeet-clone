# Specs

Spec-driven development artifacts for the Gmeet Clone.

This directory follows the [OpenSpec](https://openspec.dev) convention:
- `specs/` — source of truth (system behavior contracts)
- `changes/` — proposed modifications (one folder per change)

See `docs/gmeet-clone-specs.md` for the full specification document.
